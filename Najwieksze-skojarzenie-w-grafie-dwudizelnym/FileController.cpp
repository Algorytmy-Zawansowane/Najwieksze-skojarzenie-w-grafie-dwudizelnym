#include "FileController.h"

namespace FileController
{
    Graph FileController::ConvertInputFile(std::string fileName)
    {
        return Graph();
    }

    void FileController::Save(Graph toSave, bool hasBeenSolved, double sumOfWages, std::string fileName)
    {
    }
}
